NEXSIZ :: monitor

Observation, classification, and artefact handling.

Logger (structured campaign output), minimizer (crash
reduction), and the base Oracle trait used by plugin oracles.

Responsible for writing crashes/hangs, emitting status lines,
and deciding whether an ExecutionResult is interesting enough
to keep or deepen via NXS.
