[package]
name = "nxs-auth-bypass"
version = "1.0.0"
edition = "2021"
description = "Official NXS: protocol-aware authentication sequence bypass / injection probe"
license = "Apache-2.0"

[[bin]]
name = "nxs-auth-bypass"
path = "src/main.rs"

[dependencies]
nxs-lib = { path = "../lib" }
serde_json = "1"
