NXS :: auth-bypass

Protocol-aware unauthenticated access probe.

Injects common auth-bypass sequences (FTP, SMTP, HTTP heuristics).
Looks for unexpected success without credentials.
Exit 2 on unauthorized-looking success.

Intrusive category. Distinct from auth-escalation.
