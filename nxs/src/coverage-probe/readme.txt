NXS :: coverage-probe

Path-diversity behavioural probe.

Uses response fingerprints as a lightweight coverage proxy.
Useful when true grey-box instrumentation is unavailable.
Exit 2 when mixed behavioural classes are observed.

Intrusive category.
