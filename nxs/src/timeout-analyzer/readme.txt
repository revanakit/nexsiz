NXS :: timeout-analyzer

Hard-hang classification.

Performs multiple timed shots to distinguish true hangs from
slow responses or transient network issues.
Exit 2 when a hard hang is confirmed.

Hang category.
