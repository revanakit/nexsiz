NXS :: save-notify

Artefact archival + optional external notification.

Copies crash/hang artefacts to a durable location and can
invoke an operator-supplied command (NXS_NOTIFY_CMD).
Always exits 0 unless I/O failure.

Safe category. Used for reliable collection during long campaigns.
