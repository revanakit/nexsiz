NXS :: notify-webhook

Compact HTTP POST of event metadata.

Sends a minimal JSON payload to an operator-defined webhook
(NXS_WEBHOOK_URL). Pure-stdlib HTTP client (no HTTPS).
Useful for external alerting / SIEM integration.

External category.
