NXS :: templates/rust

Minimal Rust scaffold for a custom existence script.

Provides Cargo.toml, src/main.rs skeleton, and nxs.toml.
Intended to depend on nxs-lib when available, or stay pure-stdlib.
Build with cargo, copy the release binary into nxs/bin or
~/.nexsiz/nxs/bin/.
