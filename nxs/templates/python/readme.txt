NXS :: templates/python

Minimal Python scaffold for a custom existence script.

Provides `run` (executable entry) and nxs.toml.
Pure stdlib. Make the script executable and place it
in an NXS search path. Interpreter must be on PATH.
