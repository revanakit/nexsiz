NXS :: templates/go

Minimal Go scaffold for a custom existence script.

Provides go.mod, main.go skeleton, and nxs.toml.
Pure Go standard library only. Build with `go build`,
then install the binary into an NXS search path.
