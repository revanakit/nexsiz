[package]
name = "nxs-lib"
version = "0.1.0"
edition = "2021"
description = "Shared contract helpers for all NXS existence scripts"
license = "Apache-2.0"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
