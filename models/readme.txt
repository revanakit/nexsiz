NEXSIZ :: models

Operator-defined protocol models (JSON field trees).

Requires --features json-model. Use with -m path/to/model.json.
Contains examples for binary length-prefix, DNS, MQTT, and a
custom schema. Prefer these over hard-coded models when the
protocol layout must change without recompilation.
